import styled from "styled-components";

export const RocketStyles = styled.div`
  opacity: 0.5;
  svg {
    fill: var(--timeline);
    margin-left: -22px;
  }
  .line-dotted {
    height: 40px;
    border-left: 1.9px dashed var(--timeline);
    margin-left: 2px;
  }
`;
